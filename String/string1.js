function myFunction(str){
    let i;
    while(i<(str.length/2))
    // for( i=0;i<(str.length/2);i++){
    //    return str[i];
      return str[i];
      i++;
    // }
       
}
 let s="sourov";
  console.log(myFunction(s))